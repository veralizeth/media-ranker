<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <header class="app-header__header">
   <%# ======= TITLE ========= %>
    <h1>
      <a href="/">Media Ranker</a>
      <small>Ranking the Best of Everything</small>
    </h1>
    <%# ======= NAVBAR ========= %>
    <nav class="navbar navbar-expand-lg navbar-light bg-light app-header__nav">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <a class="navbar-brand" href="<%= root_path %>">Home</a>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-md-0">
          <li class="nav-item active">
            <a class="nav-link" href="<%= root_path %>">View top media<span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= works_path %>">View all media<span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_work_path %>">Add a new work<span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= works_path %>">View all users<span class="sr-only">(current)</span></a>
          </li>
        </ul>
      </div>
    <%# ======= LOGIN AND LOGOUT BUTTONS ========= %>
    <%if session[:user_id]%>
      <%=link_to "Logged in as #{session[:username]}", current_user_path(session[:user_id]), :class => "btn btn-primary"%>
      <%=link_to "Log out", logout_path, :class => "btn btn-primary", :method => :post %>
    <%elsif session[:id] == nil%>
      <%=link_to "Log In", login_path, :class => "btn btn-primary"%> 
    <%end%>
    </nav>
    <%# ======= FLASH MESSAGES ========= %>
    <section class="flash">
      <% flash.each do |name, message|%>
        <div class="<%= name %>">
          <%= message %>
        </div>
      <% end %>
    </section>
  </header>
  <body>
    <%= yield %>
  </body>
</html>
